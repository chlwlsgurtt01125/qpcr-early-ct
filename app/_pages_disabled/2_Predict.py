import streamlit as st
st.set_page_config(page_title="예측하기", layout="wide")
st.title("🧪 예측하기")
st.caption("파일 업로드 → cutoff 선택 → 예측 → 그래프로 결과/평가 확인")
st.info("준비 중: 업로드/파싱, 예측, true 있으면 평가 그래프/Top error 연결")
